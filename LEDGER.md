# LEDGER

Not a session log. Do not edit this file, or any other document, to record that a thread did some work. Git history already has that.

Edit a document only when one of these is true:

- A scenario in [`spec/SPEC.md`](spec/SPEC.md) is wrong or incomplete, so the code and the spec would disagree. Change that scenario, and nowhere else.
- A name on the pending list in [`docs/technical-decisions.md`](docs/technical-decisions.md) was decided. Add one short decision and remove it from the pending list.
- Something was learned that would cost real effort to reconstruct, and it is not already written down. Put that learning in the decision log. Skip it when the diff already shows it.

Do not restate the same fact in the README, the PRD, the technical design, and here.

## Still open

Phase 1 (project identity) is already implemented. Phase 2 needs the frontmatter field names and the record file-naming strategy. Both are still on the pending list. Do not invent them. Delete this note when that is no longer true.

## Standing constraints

- Never add AI-attribution lines to a commit, PR, issue, or comment. No "Co-Authored-By: Claude", no "Generated with Claude Code".
- Do not reintroduce mention of another long-term-memory-for-agents project. Those references were removed on purpose.
